workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "web"   # déclenché via UI : Run Pipeline
    - if: $CI_PIPELINE_SOURCE == "api"   # déclenché via API GitLab

stages:
  - validate
  - plan
  - infra
  - docker
  - deploy

# Secrets à définir dans GitLab → Settings → CI/CD → Variables :
#   OVH_CLIENT_ID, OVH_CLIENT_SECRET, OVH_SERVICE_NAME (non masquables)
#   OVH_S3_ACCESS_KEY, OVH_S3_SECRET_KEY              (masquables)
#
# Variable à passer au déclenchement (Run Pipeline) :
#   ENV  →  dev | prd   (défaut : dev)
variables:
  ENV:
    value: "dev"
    description: "Environnement cible (dev, prd)"
  TF_VAR_ovh_client_id: $OVH_CLIENT_ID
  TF_VAR_ovh_client_secret: $OVH_CLIENT_SECRET
  TF_VAR_service_name: $OVH_SERVICE_NAME
  TF_VAR_env: $ENV
  AWS_ACCESS_KEY_ID: $OVH_S3_ACCESS_KEY
  AWS_SECRET_ACCESS_KEY: $OVH_S3_SECRET_KEY

.terraform-image: &terraform-image
  name: hashicorp/terraform:latest
  entrypoint: [""]

.tf-init: &tf-init
  before_script:
    - terraform init -backend-config="key=ovh/$ENV/terraform.tfstate"

# ─── VALIDATE ─────────────────────────────────────────────────────────────────

terraform-validate:
  stage: validate
  image: *terraform-image
  <<: *tf-init
  script:
    - terraform fmt -check
    - terraform validate

# ─── PLAN ─────────────────────────────────────────────────────────────────────

terraform-plan:
  stage: plan
  image: *terraform-image
  <<: *tf-init
  script:
    - |
      terraform plan -no-color \
        -target=module.network \
        -target=module.kubernetes \
        -target=module.registry \
        -target=module.database

# ─── INFRA (bouton manuel) ────────────────────────────────────────────────────

terraform-infra:
  stage: infra
  image: *terraform-image
  <<: *tf-init
  script:
    - |
      terraform plan -no-color -out=tfplan \
        -target=module.network \
        -target=module.kubernetes \
        -target=module.registry \
        -target=module.database
    - terraform apply -no-color -auto-approve tfplan
  when: manual

# ─── DOCKER (bouton manuel) ───────────────────────────────────────────────────

docker-build:
  stage: docker
  image: *terraform-image
  services:
    - name: docker:dind
      alias: docker
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_TLS_CERTDIR: /certs
    DOCKER_TLS_VERIFY: "1"
    DOCKER_CERT_PATH: /certs/client
  before_script:
    - apk add --no-cache docker-cli
    - terraform init -backend-config="key=ovh/$ENV/terraform.tfstate"
  script:
    - |
      REGISTRY_URL=$(terraform output -raw registry_url)
      REGISTRY_HOST="${REGISTRY_URL#https://}"
      REGISTRY_HOST="${REGISTRY_HOST#http://}"
      REGISTRY_USER=$(terraform output -raw registry_login)
      REGISTRY_PASSWORD=$(terraform output -raw registry_password)
      echo "$REGISTRY_PASSWORD" | docker login "$REGISTRY_HOST" -u "$REGISTRY_USER" --password-stdin
      docker build -t "$REGISTRY_HOST/library/myapp:latest" .
      docker push "$REGISTRY_HOST/library/myapp:latest"
  when: manual

# ─── DEPLOY (bouton manuel) ───────────────────────────────────────────────────

terraform-deploy:
  stage: deploy
  image: *terraform-image
  <<: *tf-init
  script:
    - terraform plan -no-color -out=tfplan
    - terraform apply -no-color -auto-approve tfplan
    - terraform output -raw app_url
  when: manual
